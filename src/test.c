#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    char message[256] = "yfguwhbejfjuh93wi4j";
    char prefix[16] = "broadcast: ";
    char send[300] = strcat(prefix, message);
}